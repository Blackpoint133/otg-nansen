# Deployment

Development and test validation must precede any production consideration.
The reviewed `nansen` schema is applied to `server_otg_staging` only. No
production schema or application deployment exists. PostgreSQL integration
tests remain pending until the temporary CREATE privilege is revoked.
